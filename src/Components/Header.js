import React from "react"

function Header() 
{
    return (
        <nav className="topnav">
            <a className="active" href="#">Home</a>
            <a href="#">CV</a>
            <a href="#">About Me</a>
        </nav>
    )
}

export default Header